# docker-compose.yml
version: '3.8'

services:
  admin:
    container_name: admin_app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8085:80' # Перенаправление порта 80
    restart: always
